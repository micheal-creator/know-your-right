# Copy to .env.local and fill in to switch from DEMO mode to a real Supabase
# backend (multi-user content, cases, and live support chat). Leave unset to
# keep running fully in the browser.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
